<% @groups.each do |group| %>
<div class="medium-4 columns box-size">
  <div class="group-callout">
    <ul>
      <div class="box-spacing">
        <h3><%= link_to group.title, group_path(group) %></h3>
        <p><%= group.description %></p>
      </div>
    </ul>
  </div>
</div>
<% end %>
